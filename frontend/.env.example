# =============================================================================
# Vibgyor Payment Gateway - Frontend Environment Configuration
# =============================================================================
# Copy this file to .env and configure with your actual values
# NEVER commit the .env file to version control

# =============================================================================
# Backend API Configuration
# =============================================================================

# VITE_API_BASE_URL - The base URL of the backend API server
# This is where the frontend will send payment initiation and verification requests
# 
# Development: Use localhost with the backend port
# Production: Use your production backend domain with HTTPS
# 
# Examples:
#   Development: VITE_API_BASE_URL=http://localhost:3000
#   Production: VITE_API_BASE_URL=https://api.example.com
#
# IMPORTANT: 
#   - Do NOT include a trailing slash
#   - Production MUST use HTTPS for security
#   - This value is embedded in the frontend bundle at build time
VITE_API_BASE_URL=http://localhost:3000
